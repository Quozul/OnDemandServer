package dev.quozul.OnDemandServer.enums;

public enum ServerStatus {
    STARTING,
    STARTED,
    EMPTY,
    STOPPING,
    STOPPED,
    ERROR,
    UNKNOWN,
    STANDALONE,
    UPDATING,
    DETACHED,
    NOT_CONFIGURED,
}
