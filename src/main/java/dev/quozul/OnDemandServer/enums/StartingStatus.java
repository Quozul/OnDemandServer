package dev.quozul.OnDemandServer.enums;

public enum StartingStatus {
    STARTING,
    TOO_MUCH_RUNNING,
    NOT_ENOUGH_MEMORY,
    UNKNOWN,
    ALREADY_STARTING,
    ALREADY_STARTED,
    NOT_CONFIGURED,
}
